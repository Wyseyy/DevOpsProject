<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update the records.</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <h2>Update Record</h2>
    <form action="/update/<%= record.id %>" method="POST">
      <input type="hidden" name="_method" value="PUT">
  
      <!-- Edit Record Name -->
      <label for="Name">Record Name:</label>
      <input type="text" id="Name" name="Name" value="<%= record.Name %>" required>
  
      <!-- Edit Artist -->
      <label for="artist">Artist:</label>
      <input type="text" id="artist" name="artist" value="<%= record.artist %>" required>
  
      <!-- Edit In Stock -->
      <label for="InStock">In Stock:</label>
      <select id="InStock" name="InStock" required>
          <option value="true" <%= record.InStock ? "selected" : "" %>>Yes</option>
          <option value="false" <%= !record.InStock ? "selected" : "" %>>No</option>
      </select>
  
      <!-- Edit Release Year -->
      <label for="ReleaseYear">Release Year:</label>
      <input type="number" id="ReleaseYear" name="ReleaseYear" value="<%= record.ReleaseYear %>" min="1900" max="2025" required>
  
      <button type="submit">Update Record</button>
  </form>
  </body>
</html>